# Experiment-wide metadata and logging defaults.
experiment:
  name: TiViT-Piano GCR basic
  seed: 1332
  deterministic: true
  output_dir: /tivit/outputs

logging:
  log_dir: /tivit/logs
  checkpoint_dir: checkpoints
  tensorboard: true
  postproc_debug: true

inference:
  output_dir: /tivit/inference_results
  predict_on_train: false
